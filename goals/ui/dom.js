export function el(tag,attrs={},...children){const node=document.createElement(tag);for(const [k,v] of Object.entries(attrs)){if(k.startsWith('on'))node.addEventListener(k.slice(2).toLowerCase(),v);else if(k==='class')node.className=v;else if(k==='text')node.textContent=v;else if(v!==false&&v!=null){if(k in node&& !k.startsWith('aria-'))node[k]=v;else node.setAttribute(k,String(v));}}for(const c of children.flat(Infinity)){if(c!=null&&c!==false)node.append(c instanceof Node?c:document.createTextNode(String(c)));}return node;}
export const button=(text,fn,cls='',attrs={})=>el('button',{type:'button',class:cls,onclick:fn,...attrs},text);
export const row=(...children)=>el('div',{class:'button-row'},...children);
export function field(label,type,value,oninput,maxLength){const control=el(type==='textarea'?'textarea':'input',{type:type==='textarea'?undefined:type,value:value||'',maxLength,oninput:e=>oninput(e.target.value),rows:type==='textarea'?3:undefined});return el('label',{class:'field'},el('span',{},label),control);}
